# Day 2 hints

1. A const bookTitle needs a string. A let currentPage needs a number because it will change.
2. A declaration without a value uses let name. Assign a value later with name = value.
3. A var binding reads as undefined before its assignment line. A let binding throws instead.
4. In TypeScript, write the type after the variable name: let currentPage: number.
