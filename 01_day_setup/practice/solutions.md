# Day 1 practice reference

Expected evidence: Node and npm report supported versions, `npm.cmd install` completes, `npm.cmd run doctor` passes, and both `npm.cmd run day1:js` and `npm.cmd run day1` print their greeting. TypeScript catches an incorrect annotation during `npm.cmd run check`; JavaScript reports problems only when the code executes.
